# Netlify only (ignored by Vercel). Publish repo root, serve app at /.
[build]
  publish = "."
  command = ""

# Serve app at site root (avoids 404 even if root index is missing)
[[redirects]]
  from = "/"
  to = "/style.css/index.html"
  status = 200
  force = true
